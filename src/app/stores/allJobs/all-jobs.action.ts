import { createAction } from '@ngrx/store';

export const showLoading = createAction('[AllJobs Store] ShowLoading');
export const hideLoading = createAction('[AllJobs Store] HideLoading');
export const reset = createAction('[Counter component] Reset');
